# API environment variables
# Copy to .env and adjust

# Port for the API server (default: 3001)
PORT=3001

# Root path for all projects; required for first-run; can be set via UI
PROJECTS_BASE_PATH=

# SQLite database path (default: packages/db/data/cursor-selfhost.sqlite)
# Do NOT use :memory: — data will be lost on restart
# DATABASE_PATH=

# Cursor CLI path: "cursor" (runs cursor agent ...) or "agent" (runs agent ...)
CURSOR_CLI_PATH=cursor

# Cursor API key (optional; or use `agent login`)
# CURSOR_API_KEY=
