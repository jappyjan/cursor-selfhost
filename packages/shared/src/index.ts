/**
 * Shared types and constants for cursor-selfhost
 */

export const APP_NAME = "cursor-selfhost";

export type SourceType = "local" | "git";

export type MessageRole = "user" | "assistant";

export type SendShortcut = "enter" | "shift_enter" | "ctrl_enter";
